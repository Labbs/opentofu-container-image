name: Pull Request test build image

on:
  pull_request:
    branches:
      - main

env:
  test_version: 1.10.3

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # - name: Download opentofu binaries
      #   run: |
      #     mkdir -p /tmp/opentofu
      #     curl -L -o /tmp/opentofu/opentofu.zip https://github.com/opentofu/opentofu/releases/download/v${{ env.test_version}}/tofu_${{ env.test_version}}_linux_amd64.zip
      #     unzip /tmp/opentofu/opentofu.zip -d /tmp/opentofu
      #     ls -lah /tmp/opentofu

      - name: Build Docker image
        run: |
          docker build -t opentofu:pr-test . --build-arg TOFU_VERSION=${{ env.test_version }}
