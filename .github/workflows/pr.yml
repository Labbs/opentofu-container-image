name: Pull Request test build image

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download opentofu binaries
        run: |
          curl -L -o opentofu.zip https://github.com/opentofu/opentofu/releases/download/v${{ github.event.inputs.version }}/opentofu_${{ github.event.inputs.version }}_linux_amd64.zip
          unzip opentofu.zip
          ls -lah

      - name: Build Docker image
        run: |
          docker build -t opentofu:pr-test .
